import * as contexts from './contexts';
import * as types from './types';
import { contextFactory, resolverFactory, documentLoaderFactory } from './builder';
declare const golem: import("factory.ts").Factory<types.DocumentLoaderManager & types.IContextManager & types.IResolverManager, "documentLoader" | "addContext" | "addResolver" | "buildDocumentLoader" | "contexts" | "resolvers" | "hasResolverForUri" | "resolve">;
export { golem, contextFactory, resolverFactory, documentLoaderFactory, contexts, types, };
